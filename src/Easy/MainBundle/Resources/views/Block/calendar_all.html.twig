{% extends 'EasyMainBundle::layout.html.twig' %}
{% block pageContent %}
    <section class="section">
        <div class="content">
            <div class="content">
                <p class="title">Календарь событий</p>
                <div class="links">
                    {% for event in calendar %}
                        <a href="#popup{{ loop.index }}" class="popup">
                            {% if event.media is defined %}
                                <div style="width: 100%; height: 165px; background: url('{% path event.media, 'big' %}'); background-size: cover;">
                                </div>
                            {% endif %}
                            {{ event.name }}
                        </a>
                        <div style='display:none;'>
                            <div id='popup{{ loop.index }}' style='padding:10px; background:#fff;'>
                                <div class="inside">
                                    {% autoescape false %}
                                        {{ event.text }}
                                    {% endautoescape %}
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </div>
    </section>
{% endblock %}

{% block sideHeader %}
{% endblock %}

{% block topHeader %}
    <ul style="position: relative;">
        
    </ul>
{% endblock %}